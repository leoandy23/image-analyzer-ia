export const analyzeImageService = async (buffer: Buffer) => {
  // [TODO] → Here we will integrate OpenAI Vision

  // placeholder response
  return [{ label: "placeholder", confidence: 0.99 }];
};
